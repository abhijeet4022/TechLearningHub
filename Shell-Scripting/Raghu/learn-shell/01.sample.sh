# Whatever command we will write it will execute one by one
cat /etc/*-release

# To run the the shell scripting file
# sudo bash filename

#!/bin/bash - Knows as shebang it tells the OS that this script should be execute using the Bash Shell.

# bash -n script.sh # Dry run if no error then no output.
# bash -u script.sh # It will check for variables weather it defined or not.
# bash -x script.sh # Debug mode or Execution Traces.

