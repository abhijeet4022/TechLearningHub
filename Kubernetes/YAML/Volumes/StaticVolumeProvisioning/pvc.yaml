apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: web_pvc
  namespace: singtel
spec:
  accessMode:
    - ReadWriteMany
  storageClassName: "slow"
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi